# Contributing

Create focused changes, add tests for behavior, run `PYTHONPATH=src .venv/bin/pytest -q`, and run Terraform formatting/validation for infrastructure changes. Do not apply cloud infrastructure or commit secrets.
