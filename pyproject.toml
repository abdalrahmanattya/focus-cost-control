[project]
name = "focus-cost-control"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.115,<1", "uvicorn[standard]>=0.30,<1", "pydantic>=2.8,<3", "python-multipart>=0.0.9,<1", "sqlalchemy>=2.0,<3", "psycopg[binary]>=3.2,<4", "PyJWT[crypto]>=2.9,<3"]
[project.optional-dependencies]
test = ["pytest>=8,<9", "httpx>=0.27,<1"]
azure = ["azure-identity>=1.17,<2", "azure-servicebus>=7.12,<8", "azure-storage-blob>=12.22,<13"]
